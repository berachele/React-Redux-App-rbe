const initialState = {
    recipeTitle: "",
    href: "",
    ingredients: [],
    isFetching: false,
    error: "",
}

export const recipesReducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}